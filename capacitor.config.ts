import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ketanku.vercel.app',
  appName: 'Ketan',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
